suites:
    default:
        src_path: src
        spec_prefix: spec
        namespace: SidekiqJob\
        psr4_prefix:

extensions:
  LeanPHP\PhpSpec\CodeCoverage\CodeCoverageExtension:
    format:
      - clover
      - xml
    output:
      clover: coverage.xml
      xml: coverage
