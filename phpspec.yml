suites:
    default:
        src_path: src
        spec_prefix: spec
        namespace: SidekiqJob\
        psr4_prefix:

extensions:
    - PhpSpec\Extension\CodeCoverageExtension

code_coverage:
    format:
        - html
        - clover
    output:
        html: coverage
        clover: coverage.xml